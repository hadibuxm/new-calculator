#include <stdio.h> /* Pull in standard I/O declarations so we can use printf */

int main(void) /* main is the entry point where program execution begins */
{
    printf("Hello World\n");
    return 0; /* Returning 0 tells the operating system the program ended successfully */
}
